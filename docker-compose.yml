services:
  fumimi:
    image: fumimi-discord:latest
    build: .
    restart: unless-stopped

    environment:
      APP_ENV: development

    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/up"]

    volumes:
      - .:/app
